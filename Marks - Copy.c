#include<stdio.h>
void main()
{
	//variable declaration
	float phy, che, mat, eng, tot;
	
	//initialization
	phy = 60.5454;
	che = 80.586;
	mat = 75.258;
	eng = 90.365;
	tot = phy+che+mat+eng; 
	
	//output
	 printf("marks in phy : %f\n",phy);
	 printf("marks in che : %f\n", che);
	 printf("marks in mat : %f\n", mat);
	 printf("marks in eng : %f\n", eng);
	 printf("total : %f", tot);

}
